Optimization:
1.draw mini-map as chunks (greedy method) rather than each block (not necessary)
2.player angle as int
3.Directly access cos / sin table (precompute)
4.precompute variables 
5.toggle map



Important
1.Refer to degrees / non essential decimals as int24_t

//optimize trig.h
